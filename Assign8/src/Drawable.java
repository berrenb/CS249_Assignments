/*
 Created by Bryan Berrent
 
 The Drawable interface contains a drawToMap method
 which will throw an exception if errors occur
 */
public interface Drawable {
	public abstract void drawToMap(Map screen);
}
